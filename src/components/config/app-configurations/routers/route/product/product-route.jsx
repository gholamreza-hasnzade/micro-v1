// * Import components
import { ProductView } from "@components/views";

const ProductRoute = [
    {
        path: "/product",
        element: <ProductView />,
        exact: true,
    },
];

export default ProductRoute;
